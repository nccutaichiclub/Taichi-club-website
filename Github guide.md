# 給網站編輯者的新手指南

這份文件是寫給「平常只在後台改文字圖片，但偶爾被要求要換某個程式碼檔案」的人看的。不需要懂任何程式語言，跟著步驟做就好。

如果你只是要改文字、換照片，**請直接用後台系統**（`https://nccutaichiclub.github.io/Taichi-club-website/admin/`），不需要看這份文件。這份文件是給「後台沒有的欄位、需要直接換檔案」的情況準備的（例如有人——像社團請 AI 工具——幫你做了新版的網頁檔案，請你上傳上去）。

---

## 目錄

1. [GitHub 是什麼，白話說明](#github-是什麼白話說明)
2. [這些檔案到底是什麼](#這些檔案到底是什麼)
3. [實際操作：怎麼「換掉」一個檔案](#實際操作怎麼換掉一個檔案)
4. [實際操作：怎麼直接在網頁上改文字](#實際操作怎麼直接在網頁上改文字)
5. [怎麼確認網站有沒有更新成功](#怎麼確認網站有沒有更新成功)
6. [不小心改壞了怎麼辦](#不小心改壞了怎麼辦)
7. [什麼時候該找人幫忙](#什麼時候該找人幫忙)

---

## GitHub 是什麼，白話說明

GitHub 可以想成是一個「雲端資料夾」，跟 Google 雲端硬碟很像——網站的所有檔案（網頁、圖片、設定檔）都放在這個雲端資料夾裡，社團網站顯示出來的畫面，就是直接讀取這個資料夾裡的東西。

跟 Google 雲端硬碟最大的不同是：GitHub 會**記錄每一次修改的歷史紀錄**，就算改壞了，也一定找得回改壞之前的樣子（後面會教怎麼做）。

社團網站的 GitHub 資料夾網址（叫做「repo」，唸作 repository）是：

> `https://github.com/nccutaichiclub/Taichi-club-website`

打開這個網址，就會看到一排排的檔案跟資料夾，跟平常看 Google 雲端硬碟的畫面感覺類似。

---

## 這些檔案到底是什麼

打開上面那個網址，你會看到類似這樣的清單：

| 你看到的東西 | 白話說明 |
|---|---|
| `index.html` | **首頁**這個網頁本身的「骨架」 |
| `gongfa.html` | **法門深造**頁面的骨架 |
| `activities.html` | **活動介紹**頁面的骨架 |
| `calendar.html` | **修煉日程**頁面的骨架 |
| `faq.html` | **常見問題**頁面的骨架 |
| `js` 資料夾（點進去會看到 `.js` 結尾的檔案） | 網頁的「操作邏輯」，例如導覽列怎麼運作、中英文切換怎麼運作。可以想成是網頁的「大腦」 |
| `css` 資料夾（`.css` 結尾） | 網頁的「外觀設計圖」，決定顏色、間距、字體大小這些 |
| `data` 資料夾（`.json` 結尾） | 網頁實際顯示的**文字內容**，平常你在後台改的東西，最後都是存在這裡 |
| `admin` 資料夾 | 後台系統本身的設定 |
| `assets` 資料夾 | 後台上傳的圖片、檔案，自動存放的地方，不用手動管理 |

**你平常只要記住一件事**：如果有人（社團夥伴、或幫忙的 AI 工具）給你一個檔案，說「請把這個換到 GitHub 上」，你只需要知道**這個檔案的名字**（例如 `calendar.html`），然後照下面的步驟把它換上去就好，完全不用懂裡面寫了什麼。

---

## 實際操作：怎麼「換掉」一個檔案

這是最常見的情境：別人給你一個新版的檔案（例如新的 `calendar.html`），請你上傳覆蓋舊的。

### 步驟

1. 打開 `https://github.com/nccutaichiclub/Taichi-club-website`，用你的 GitHub 帳號登入（如果還沒有帳號，右上角有「Sign up」可以免費註冊）
2. 找到要換掉的那個檔案所在的位置。如果檔案名字是 `calendar.html`，它會直接在最外層那排清單裡；如果是 `lang.js`，要先點進 `js` 資料夾才看得到
3. **點進資料夾**的方式：直接點資料夾的名字（藍色文字）就會進去
4. 進到正確的資料夾之後，找到跟你手上新檔案**同名**的舊檔案
5. 點進畫面右上角一排小按鈕裡的「**Add file**」→ 選「**Upload files**」（如果沒看到，畫面可能會直接顯示一個上傳區域，把檔案拖進去也可以）
6. 把你手上的新檔案，**用滑鼠拖曳**到網頁中間那個虛線框框裡（或點框框裡的文字選擇檔案）
7. 拖上去之後，往下捲動，畫面最下面會有一個綠色的「**Commit changes**」按鈕（意思是「確認提交這次修改」）
8. 確保有選到「**Commit directly to the `main` branch**」這個選項（通常是預設選好的）
9. 點下綠色的「Commit changes」按鈕，就完成了！

> **重要提醒**：檔案名字一定要**完全一樣**（包含大小寫），GitHub 才會知道是要「取代」舊檔案，而不是「多一個新檔案」。如果名字打錯或不同，網站可能會顯示錯誤或沒反應。

### 如果要換的檔案有好幾個

同樣的步驟重複做就好，一次也可以同時拖好幾個檔案上去（前提是這幾個檔案剛好都放在同一個資料夾裡）。如果檔案分別要放在不同資料夾（例如一個要放根目錄、一個要放進 `js` 資料夾），就要分兩次做，先進到正確的資料夾再上傳。

---

## 實際操作：怎麼直接在網頁上改文字

如果只是要改**一兩個字**（例如網址打錯、錯字），不需要重新上傳整個檔案，可以直接在 GitHub 網頁上編輯：

1. 找到要改的檔案，**點進去**（不是資料夾，是點檔案本身）
2. 畫面右上角會有一支「鉛筆」圖示的按鈕，點下去就會進入編輯模式
3. 這時候畫面會變成可以直接打字修改的樣子，找到要改的地方，直接修改文字
4. 改好之後，往下捲動到最下面，一樣會看到綠色的「**Commit changes**」按鈕
5. 點下去確認送出

> **提醒**：這個方式因為是直接改程式碼裡的文字，如果不小心改到不該動的符號（例如引號、括號），可能會讓網頁跑不出來。**如果不確定要改的地方安不安全，建議還是把檔案內容複製給熟悉的人或 AI 工具看過，請他們幫你確認或直接給你新檔案，用上一節「換掉檔案」的方式處理比較保險。**

---

## 怎麼確認網站有沒有更新成功

送出修改之後（不管是上傳新檔案還是直接編輯），GitHub 會自動把新版本的網站重新產生，這個過程通常需要**等 30 秒到 2 分鐘**。

確認方式：
1. 打開網站正式網址：`https://nccutaichiclub.github.io/Taichi-club-website/`
2. 如果畫面看起來沒變，先**強制重新整理**（Windows 按 `Ctrl + F5`，Mac 按 `Cmd + Shift + R`），因為瀏覽器有時候會顯示快取的舊畫面
3. 如果等了 5 分鐘還是沒變化，才需要擔心是不是哪裡出錯了

---

## 不小心改壞了怎麼辦

不用緊張，GitHub 會保留每一次修改的完整歷史，一定找得回來：

1. 打開那個「改壞了」的檔案，點進去
2. 在檔案上方會看到「**History**」（歷史紀錄）的按鈕或連結，點進去
3. 會看到一排列表，是這個檔案每一次被修改的紀錄，越上面越新
4. 點選「改壞之前」的那一次紀錄，就可以看到當時的內容
5. 把那個版本的內容複製下來，用「直接在網頁上改文字」的方式，貼回去覆蓋現在壞掉的內容，再 Commit 一次即可

如果真的找不到怎麼復原，**先不要慌張亂點**，把情況（是哪個檔案、大概改了什麼）截圖或描述清楚，找熟悉的夥伴或 AI 工具幫忙看歷史紀錄，一定救得回來，不會整個網站消失。

---

## 什麼時候該找人幫忙

**可以自己嘗試的情況：**
- 別人已經給你完整的新檔案，你只要負責「上傳覆蓋」
- 改網頁裡看得懂的一般文字內容（例如中文句子、社團名稱），且前後都是完整的中文字，沒有夾雜一堆符號
- 只是想確認網站有沒有更新成功

**建議先找人幫忙的情況：**
- 需要新增一個全新的功能或頁面（不是「換檔案」，是「要設計新的東西」）
- 錯誤訊息看不懂、網站整頁空白或跑版
- 需要修改 `data/*.json`、`admin/config.yml` 這類檔案，但沒有透過後台系統（這些檔案格式要求很嚴格，多一個符號少一個逗號就可能讓整頁讀不出來）
- 不確定某個改動會不會影響到其他頁面

遇到以上情況，把畫面截圖、想達成的目標講清楚，交給比較熟悉的社團夥伴，或是把現有檔案內容貼給 AI 工具（例如 Claude）幫忙產生新的檔案，你只要負責最後「上傳覆蓋」這個動作就好。

---

*這份文件是給不熟悉程式的網站編輯者看的入門指南。如果你是比較熟悉技術、或想了解整個網站架構細節的人，請看同一個資料夾裡的 `README.md`。*
